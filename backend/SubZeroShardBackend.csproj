<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>SubZeroShardBackend</RootNamespace>
    <!-- This project is intentionally OUTSIDE the s&box `Code/` folder so the s&box
         editor does not try to compile it. It is a standalone ASP.NET service. -->
  </PropertyGroup>

</Project>
